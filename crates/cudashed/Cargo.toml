[package]
name = "cudashed"
version = "0.1.0"
edition = "2024"

[dependencies]
cudarc = { version = "0.17.3", features = ["cuda-version-from-build-system"] }
color-eyre = "0.6.5"
itertools = "0.14.0"

[lints]
workspace = true
